# Stock-Price-Forcasting
ARIMA + LSTM hybrid stock forecasting with walk-forward validation, naive-baseline benchmarking, and a trading backtest built to test whether the hybrid actually beats a random walk, not just report a low error number.
